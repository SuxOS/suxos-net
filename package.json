{
  "name": "suxos-net",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "echo 'STAGING ONLY — not wired for a real deploy yet; see README.md. Do not run wrangler deploy against this config without a real KV namespace + Access policy.' && exit 1",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260710.1",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.111.0"
  }
}
